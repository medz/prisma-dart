/// A class representing a null value in Prisma.
///
/// Unlike Dart `null`, which is used to represent a value that should be
/// ignored, sometimes `null` is a valid empty value in Prisma or a database.
/// In such cases, [PrismaNull] can be used to represent it.
class PrismaNull {
  const PrismaNull();
}
