name: prisma_with_sqlite
description: A sample command-line application.
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.5.0

# Add regular dependencies here.
dependencies:
  orm:
    path: ../../packages/orm

dev_dependencies:
  lints: ^6.0.0
