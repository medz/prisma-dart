name: orm_flutter_ios
description: "Prisma Query Engine implementation for iOS platform (FFI)"

resolution: workspace
version: 0.0.1

environment:
  sdk: ^3.5.0
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  plugin:
    implements: orm_flutter
    platforms:
      ios:
        ffiPlugin: true
        dartPluginClass: Demo
